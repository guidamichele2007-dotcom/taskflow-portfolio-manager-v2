{
  "name": "rootguard-design-and-scaffold",
  "version": "0.1.0",
  "private": true,
  "description": "GDD, dataset e prototipo del Simulation Core di ROOTGUARD: L'Ultima Fioritura (vedi docs/gdd).",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
